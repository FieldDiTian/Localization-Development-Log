# 多激光雷达-IMU车辆地图定位问题定义与优化方向

## 摘要

本文面向一套由左、中、右三个激光雷达、实时 IMU、先验三维点云地图及相关定位算法组成的车辆地图定位系统，对任务目标、系统边界、现有模块、运行流程、失效模式、约束条件与优化方向进行统一定义。

系统首先在离线阶段利用 GLIM、三路激光雷达、IMU 及 INS ground truth 构建高质量三维点云地图；在在线定位阶段，仅允许使用三路激光雷达、实时 IMU、DLIO 输出、GICP 及先验地图估计车辆位姿。Gating 模块负责判断每帧 GICP 匹配结果是否可信，但在运行时绝不能读取或间接使用 INS/GPS ground truth。

当前系统的主要风险并非单次匹配失败，而是错误匹配被 gating 接受后进入正式定位状态，并作为后续匹配的初始条件，造成误差持续传播、轨迹逐渐偏离，最终只能通过实验性的 snap-to-INS 机制恢复。本文据此提出覆盖地图、标定与同步、点云处理、DLIO 运动先验、GICP、gating、状态管理、重定位和地图维护的系统性优化方案。

---

## 1. 任务背景与目标

本项目研究车辆在已有三维点云地图中的实时定位问题。车辆搭载三个激光雷达和一个实时 IMU，系统希望在 GPS 或 INS 不可用的情况下，依靠激光雷达、IMU 及先验地图，持续估计车辆在地图坐标系中的位置和姿态。

项目采用“离线建图、在线定位、离线评价”三条相互隔离的信息链路：

1. 离线建图阶段允许使用 INS ground truth，以获得尽可能准确、一致的先验点云地图。
2. 在线定位阶段不得使用 INS/GPS ground truth，只能使用三路激光雷达、实时 IMU、先验地图及算法内部信息。
3. 离线评价阶段允许将定位结果与 INS ground truth 比较，用于分析误差、标注匹配成败和优化参数。

最终工程目标是在没有 GPS/INS 帮助的条件下，使系统能够完成正常跟踪、错误匹配检测、失效判断以及自主重定位。当前阶段的直接目标是降低错误 GICP 匹配被接受的概率，阻止错误状态持续传播，并延长系统从正确初始状态开始后的稳定定位时间和距离。

## 2. 系统现有输入与模块

### 2.1 左、中、右三个激光雷达

车辆装有左雷达、中雷达和右雷达。三套雷达从不同方向观测车辆周围环境，提供实时三维点云。

三个雷达的外参应当已知，使各自点云能够转换至统一的车辆坐标系或指定参考雷达坐标系。三雷达既可以形成融合点云用于整体地图匹配，也应保留各雷达的来源信息，以便分析每个雷达对匹配结果的独立支持程度。

### 2.2 实时 IMU

IMU 是车辆运行过程中真实采集的实时惯性数据，不是由 INS 派生的数据。其坐标轴定义为：

- X 轴指向车辆前方；
- Y 轴指向车辆侧向；
- Z 轴指向上方。

IMU 使用 ROS Time；激光雷达点云记录于 ROS Bag 时间体系。二者处于同一软件时间体系并不必然表示物理采样完全同步，因此仍需验证消息时间戳语义、固定延迟、扫描参考时刻以及帧内点时间。

IMU 可用于短时运动估计、状态传播、点云运动补偿以及 GICP 初值生成。即使 IMU 数据质量有限或存在噪声、零偏和积分漂移，其短时间运动趋势仍可能为匹配判断提供重要参考。

### 2.3 GLIM 离线建图

GLIM 用于离线构建三维先验点云地图。建图阶段可以使用：

- 左、中、右三个激光雷达点云；
- 实时 IMU；
- INS ground truth。

离线建图的目的不是证明系统能够脱离 INS 定位，而是利用可用的高精度参考轨迹，尽可能降低地图中的轨迹误差、重影、扫描畸变和跨时段不一致，为后续在线地图定位提供可靠基准。

### 2.4 DLIO

DLIO 是激光雷达—惯性里程计模块。它可利用激光雷达与 IMU 估计连续或短时相对运动，并支持逐点去畸变。

在本系统中，DLIO 不应被视为地图绝对定位的替代方案，而应承担以下辅助作用：

- 对扫描点云进行运动补偿；
- 为 GICP 提供较合理的初始位姿；
- 提供帧间相对运动趋势；
- 与地图 GICP 的绝对匹配结果进行一致性检查；
- 在地图匹配短时退化时维持有限时间的相对运动估计。

DLIO 会随时间或行驶距离积累漂移，因此不能作为无限期有效的绝对位置来源。

### 2.5 GICP

GICP 是在线定位中的核心点云配准算法。它将当前车辆点云与先验地图中的局部子地图进行匹配，输出候选位姿更新。

GICP 属于局部优化方法，依赖合理的初始位姿、足够的点云重叠和具有辨识度的几何结构。算法报告“已收敛”或具有较低残差，并不必然表示其收敛到了全局正确位置；在重复结构、动态环境或几何退化场景中，GICP 可能收敛到错误的局部最优解。

### 2.6 Gating

Gating 接收 GICP 的候选匹配结果，并判断该结果是否可以写入正式定位状态。

若结果被接受，系统使用该位姿更新车辆位置，并将其作为后续帧的定位基础。若结果被拒绝，系统不应将该候选结果直接写入正式状态，同时累计连续拒绝次数。

Gating 的关键任务不是单纯提高接受率，而是在不使用 ground truth 的前提下，尽可能降低错误匹配的接受率，同时控制对正确匹配的过度拒绝。

### 2.7 INS ground truth

车辆具备较准确的 GPS/INS 系统，其轨迹在本项目中被定义为 ground truth。它是地图生产、实验分析和离线评价的参考，不是在线定位系统正常运行时可见的传感器输入。

当前实验系统还会在连续拒绝达到阈值后，将定位状态 snap 到 INS 轨迹。这一机制可用于实验恢复和持续采集数据，但不属于最终可部署定位能力，因为真实 GPS/INS 失效时将不存在可供 snap 的 ground truth。

## 3. Ground truth 的允许用途与禁止用途

### 3.1 允许用途

INS/GPS ground truth 可以用于：

- 辅助 GLIM 离线建图；
- 将多次采集的点云对齐至统一且准确的地图坐标系；
- 离线评价车辆定位的平移、姿态、横向、纵向和航向误差；
- 离线标注每帧 GICP 匹配实际正确或错误；
- 统计 gating 的错误接受、错误拒绝、正确接受和正确拒绝；
- 分析失效发生前的点云、DLIO、GICP 和 gating 内部指标；
- 训练或调试不在运行时读取 ground truth 的质量判断模型；
- 选择阈值和完成参数敏感性分析；
- 在当前实验系统中执行故障后的数据采集重置，但必须明确标记该段状态；
- 作为独立的测试基准，验证系统在未见路线和不同环境中的泛化能力。

### 3.2 禁止用途

在线运行时，INS/GPS ground truth 不得用于：

- 判断当前 GICP 结果是否正确；
- 计算 gating 的接受或拒绝决定；
- 生成 gating 的任何直接或间接输入特征；
- 为 GICP 提供真实位姿初值；
- 在正常跟踪过程中修正车辆状态；
- 选择局部地图、搜索窗口或候选位置；
- 判断是否发生定位丢失；
- 在最终部署系统中完成重定位；
- 通过与真实轨迹比较来动态调整阈值；
- 以任何数据泄漏形式进入在线决策链路。

必须特别防止间接泄漏。例如，由 ground truth 预计算并随数据帧附带的误差标签、真实位姿相关分数、基于真实位置裁剪的子地图以及利用未来 ground truth 得到的阈值，均不得作为在线输入。

## 4. 当前系统运行流程与失效模式

### 4.1 离线建图流程

1. 采集三路激光雷达、实时 IMU 和 INS 数据。
2. 完成三雷达之间以及雷达、IMU、车体之间的标定与时间对齐。
3. 使用 IMU 或 DLIO 进行运动估计和点云去畸变。
4. 通过 GLIM 结合激光雷达、IMU 和 INS 约束优化轨迹。
5. 累积点云并生成统一坐标系下的三维地图。
6. 对地图进行动态点清理、下采样、法向或局部结构计算和分区管理。
7. 输出供在线 GICP 使用的定位专用地图。

### 4.2 当前在线定位流程

1. 读取当前时段的三路激光雷达和实时 IMU 数据。
2. 根据外参将三路点云转换至统一坐标系，同时保留雷达来源信息。
3. 使用 IMU 和 DLIO 进行点云运动补偿，并预测当前短时运动。
4. 根据上一可信位姿或预测位姿提取局部子地图。
5. 使用预测位姿作为初值执行 GICP。
6. GICP 输出候选位姿及相关匹配质量信息。
7. Gating 在不读取 INS/GPS ground truth 的情况下接受或拒绝候选结果。
8. 若接受，候选位姿被写入正式定位状态，并用于下一帧定位。
9. 若拒绝，系统累计连续拒绝次数，并使用保守的短时传播策略维持状态。
10. 当前实验设计中，当连续拒绝达到设定上限后，系统 snap 到 INS ground truth，随后重新开始下一轮定位。

### 4.3 主要失效模式

当前最危险的失效模式是“错误接受及其传播”：

1. GICP 因初值不佳、重复结构、动态目标、地图变化或几何退化而收敛到错误局部最优解。
2. 错误解在局部残差、对应点数量或收敛标志上可能看似正常。
3. Gating 错误地接受该结果。
4. 错误位姿被写入正式状态，并成为下一帧局部地图提取和 GICP 初始化的基础。
5. 后续匹配在错误位置附近继续优化，系统逐步偏离真实轨迹。
6. 偏差扩大后，当前扫描与正确地图区域的重叠下降，匹配开始连续失败。
7. Gating 连续拒绝，达到阈值后，实验系统 snap 回 INS 轨迹。
8. 系统重新进入正常定位，之后可能再次重复上述循环。

此外还可能存在以下失效模式：

- 时间不同步或时间戳语义错误导致运动补偿失真；
- 多雷达外参误差导致融合点云出现重影；
- 点云帧内时间未正确使用，在高速运动或转弯时产生形变；
- 地图保留了车辆、行人、植被或临时设施等不稳定结构；
- 三路雷达被简单拼接，局部异常被大量点数掩盖；
- GICP 过度依赖路面或单一墙面，部分运动方向缺乏约束；
- 固定搜索范围和阈值无法适应不同速度、环境与不确定性；
- 单帧接受立即污染状态，系统缺乏候选确认和回滚能力；
- 只有“接受/拒绝”二值状态，无法表达退化、可疑、丢失和重定位；
- 连续拒绝后只能依赖 INS 恢复，缺少自主重定位能力。

## 5. 问题定义

本项目研究多激光雷达—IMU车辆在先验三维点云地图中的实时定位问题。

离线阶段，系统使用左、中、右三个激光雷达、实时 IMU 及 INS ground truth，通过 GLIM 构建准确且适合定位的三维点云地图。

在线阶段，系统只能使用三个激光雷达的实时点云、实时 IMU、DLIO 提供的短时运动信息、GICP、先验地图和算法内部历史状态，估计车辆在地图坐标系中的位置与姿态。每帧 GICP 候选匹配由 gating 判断是否可信。Gating 在运行时不得读取或间接利用 INS/GPS ground truth。

研究重点是在动态目标、地图变化、有限点云重叠、重复结构、几何退化、传感器噪声以及初值存在误差的条件下：

- 降低 GICP 收敛到错误局部最优解的概率；
- 准确识别错误或退化匹配；
- 防止错误候选立即污染正式定位状态；
- 在定位丢失时作出明确判断；
- 最终在不依赖 INS/GPS 的条件下完成自主重定位；
- 保持满足车辆系统需求的实时性、连续性和定位精度。

该问题不是一般的无地图激光雷达—惯性里程计问题，而是“多激光雷达—IMU融合的先验点云地图定位与失效管理问题”。

## 6. 现有条件与基本假设

### 6.1 已有条件

- 车辆搭载左、中、右三个激光雷达；
- 车辆搭载一个实时 IMU；
- IMU 坐标轴方向已知；
- 激光雷达与 IMU 数据均记录在 ROS 数据体系中；
- 已有较准确的 GPS/INS 轨迹，可作为 ground truth；
- 已有 GLIM 离线建图能力；
- 已有 DLIO 激光雷达—惯性里程计能力；
- 已有 GICP 点云匹配能力；
- 已有对匹配结果进行接受或拒绝的 gating 系统；
- 已有连续拒绝阈值和实验性 snap-to-INS 恢复机制；
- 可在离线阶段使用 ground truth 评价和标注系统行为。

### 6.2 传感器与标定假设

- 三个雷达与车体之间为刚性连接；
- IMU 与车体之间为刚性连接；
- 三雷达及 IMU 外参已知，或可通过标定获得足够准确的结果；
- 各传感器坐标轴、单位和方向定义明确；
- 点云具有正确的帧时间戳，且最好包含点级相对时间；
- 可估计或校正传感器间固定时间偏移和消息延迟；
- IMU 在短时间内可提供有意义的运动趋势，即使长期积分会漂移。


## 7. 关键约束

### 7.1 Ground truth 隔离约束

在线 gating 运行时绝不能使用 INS/GPS ground truth。这是本项目最重要、不可放宽的约束。

该约束不仅禁止直接读取 INS/GPS 位姿，也禁止通过预先计算的真实误差、由真实位置裁剪的地图、ground truth 派生特征或未来真实轨迹等方式间接泄漏信息。

### 7.2 输入边界约束

在线定位允许使用的信息仅包括：

- 三路激光雷达点云及其内部质量信息；
- 实时 IMU；
- DLIO 输出及其内部可用质量指标；
- 先验定位地图；
- GICP 候选结果和内部统计；
- 历史已接受状态、候选状态及系统自身维护的不确定性；
- 不依赖 INS/GPS 的车辆运动学信息，但仅在系统实际可获得时使用。

### 7.3 实时性约束

点云处理、局部地图提取、GICP、gating 和状态更新必须在传感器数据到达速率下持续运行。除平均耗时外，还需控制长尾延迟，避免偶发超时导致输入堆积或状态滞后。

### 7.4 因果性约束

在线系统只能使用当前及过去数据，不能使用未来帧信息作出当前实时输出。若采用多帧确认，可允许受控延迟，但必须明确候选输出和正式确认输出之间的区别。

### 7.5 安全性约束

错误接受通常比错误拒绝代价更高。错误拒绝可能导致短时缺少地图校正，而错误接受会污染后续初值并引发持续漂移。因此 gating 的阈值、损失和评价指标应体现这种非对称风险。

### 7.6 Snap-to-INS 的边界

Snap-to-INS 只能被视为当前实验中的恢复、分段和数据采集机制，不能计入最终系统的自主定位能力。所有性能报告应明确区分 snap 前后区段，并同时报告无 snap 条件下的失效表现。

## 8. 可优化方向

### 8.1 面向定位的地图质量

优化 GLIM 地图不能只追求点云更密或轨迹更接近 INS，还应提高地图对在线定位的适用性：

- 改善全局轨迹一致性，减少重复经过区域的错层和重影；
- 校正三雷达外参和时间关系；
- 提高高速运动与转弯过程中的去畸变质量；
- 移除车辆、行人和临时障碍物；
- 降低植被等季节性或不稳定结构的权重；
- 保留建筑立面、固定路缘、杆体、桥墩和隧道结构等长期稳定特征；
- 控制路面点比例，防止大量平面点淹没更有辨识度的结构；
- 为地图点计算可靠的法向、局部协方差和稳定性标签；
- 建立分层、分区和多分辨率地图，以支持快速局部查询和粗到细匹配；
- 将“高精度展示地图”和“定位专用地图”分离管理。

### 8.2 时间同步、外参与去畸变

这一层会同时影响 GLIM、DLIO、GICP 和 gating，应作为基础工程重点：

- 核实 ROS Time、ROS Bag 记录时间和传感器采样时间的关系；
- 确认点云时间戳表示扫描开始、扫描结束还是其他参考时刻；
- 检查三个雷达之间的固定时间偏移；
- 检查 IMU 消息延迟及采样时间语义；
- 使用点级时间对旋转式雷达逐点去畸变；
- 校验 IMU 坐标轴方向、角速度和加速度单位；
- 通过静止、匀速直行、转弯等场景检查融合点云是否重合、墙面是否分层；
- 评估车辆振动、安装结构变形和温度变化对外参稳定性的影响。

### 8.3 三雷达利用方式

三个雷达不应只在预处理阶段被无差别拼接。建议：

- 在融合点云中保留每个点的雷达来源；
- 分别统计左、中、右雷达的有效对应点、重叠率和匹配残差；
- 判断对应点是否集中于单一雷达或局部区域；
- 检查三雷达对位移和航向修正的支持是否一致；
- 根据各雷达视场、遮挡、噪声和环境结构动态赋权；
- 当某个雷达被遮挡、污染或处于退化方向时降低其影响；
- 将三雷达内部一致性作为 gating 的重要输入；
- 在关键失效数据上比较“整体融合匹配”和“分雷达独立验证”的表现。

### 8.4 DLIO 运动先验

DLIO 可从单纯的去畸变工具扩展为短时运动约束来源：

- 为 GICP 提供帧间运动初值；
- 根据 DLIO 不确定性动态设置 GICP 搜索范围；
- 比较 GICP 与 DLIO 的位移方向、转角和速度变化；
- 当二者冲突时将结果标记为可疑，而不是直接相信任一方；
- 在地图匹配被拒绝时，短时间使用 DLIO 传播，但同步增加不确定性；
- 限制纯 DLIO 传播的最长时间或距离；
- 将 DLIO 退化指标、残差和状态不确定性输入 gating；
- 检查 DLIO 与 GICP 共享点云造成的统计相关性，避免把两者误当作完全独立证据。

### 8.5 GICP 匹配算法

GICP 的优化重点应放在降低错误局部最优和提高候选可辨识度：

- 采用由粗到细的多分辨率、体素化匹配；
- 使用多个初始位姿，而非只优化单一初值；
- 保留多个候选解，并比较其分数差距和时间一致性；
- 根据车辆速度和状态不确定性动态调整搜索范围；
- 使用鲁棒异常值抑制，降低动态目标和错误对应的影响；
- 改善对应点筛选，限制不合理距离、法向和局部几何关系；
- 平衡地面、立面、边缘、杆状结构等不同几何类型的贡献；
- 评估约束是否覆盖所有关键运动方向；
- 识别几何退化，并在弱约束方向降低更新幅度或拒绝更新；
- 输出丰富的匹配诊断信息，而不仅是单一总分和收敛标志；
- 使用最近若干帧形成局部扫描子地图，提高单帧点云的稳定性和辨识度；
- 对失败路段建立专门的回放测试集，进行参数和算法版本对比。

### 8.6 多指标 Gating

Gating 不应只依赖 GICP 最终分数、残差、对应点数量或收敛标志。建议综合以下证据：

- GICP 是否正常收敛以及优化过程是否稳定；
- 最终残差及残差分布，而非仅平均值；
- 当前扫描和局部地图的实际重叠率；
- 有效对应点数量及其空间分布均匀性；
- 左、中、右三个雷达是否共同支持该结果；
- GICP 相对 DLIO/IMU 预测所需修正是否合理；
- 位移、转角、速度和加速度是否符合车辆短时运动连续性；
- 当前环境是否在各关键运动方向上提供足够约束；
- 最优候选与次优候选之间是否具有足够明显的区分度；
- 最近多帧是否持续支持同一候选轨迹；
- 局部地图是否包含足够稳定且有辨识度的结构；
- 当前传感器质量是否异常，例如点数骤降、遮挡、雨雾或振动。

Gating 应输出置信度、风险等级或原因码，而不仅是一个布尔结果。原因码可用于区分低重叠、运动冲突、几何退化、多雷达不一致和优化异常等情况。

离线阶段可利用 INS ground truth 标注数据、训练分类器或校准置信度，但必须按路线、日期和场景划分训练集与测试集，防止同一路段数据泄漏造成过度乐观结果。部署时模型输入只能来自允许的在线信息。

### 8.7 错误传播控制

当前“接受一次即写入正式状态”的机制会放大单帧误判。建议引入：

- 最后可信位姿和最后可信局部地图；
- 候选状态与正式状态分离；
- 一至数帧的暂定—确认机制；
- 多帧支持后再正式提交较大位姿修正；
- 对异常大修正采用限幅、分阶段更新或额外验证；
- 保存近期原始点云、候选位姿和轨迹历史；
- 当候选后续失去支持时回滚到最后可信状态；
- 对连续拒绝期间的状态不确定性显式增长；
- 防止使用被拒绝候选裁剪下一帧局部地图；
- 区分“未获得新地图校正”和“已经确认定位错误”。

### 8.8 定位状态机

建议将单一接受/拒绝逻辑升级为明确状态机：

1. **正常跟踪**：GICP、DLIO、多雷达与运动一致性均正常，允许常规更新。
2. **退化跟踪**：环境约束不足或部分指标异常，限制更新并降低置信度。
3. **可疑状态**：候选结果与运动先验或多雷达证据冲突，暂不写入正式状态，等待后续确认。
4. **定位丢失**：系统已无法给出可信的地图位姿，停止将当前结果标记为有效定位。
5. **重定位**：扩大搜索范围，生成和验证多个全局候选。
6. **恢复确认**：候选重定位结果需经多帧确认后，才能返回正常跟踪。

状态转换应由透明、可记录的条件触发，并设置必要的进入与退出滞回，避免在临界条件下频繁切换。

### 8.9 无 INS/GPS 自主重定位

最终系统不能依赖 snap-to-INS。建议建立“地点检索、粗配准、精配准、时序确认”的重定位链路：

- 使用最近若干帧构建局部点云子地图；
- 从全局地图检索多个可能位置；
- 对候选执行粗粒度几何对齐；
- 使用 GICP 进行精细优化；
- 利用三雷达一致性、DLIO 相对运动和多帧观测排除错误候选；
- 当候选在连续多帧中稳定成立后恢复正式定位；
- 对重复结构场景保留多假设，直至获得足够区分信息；
- 明确重定位超时、失败和降级行为。

### 8.10 地图维护

地图即使在建图时很准确，也会随环境变化逐步失效。建议：

- 标记长期稳定、可能变化和动态结构；
- 定期统计各地图区域的匹配残差与失效率；
- 识别道路施工、设施变化、植被变化和长期遮挡；
- 对问题区域重新建图或局部更新；
- 维护地图版本、生成日期、覆盖范围和传感器配置；
- 必要时维护不同季节或不同运营状态的地图版本；
- 防止在线短期动态物体未经验证直接写入永久地图；
- 建立地图更新后的回归测试和兼容性验证流程。

## 9. 建议的研发优先级

### 第一优先级：建立离线诊断与评价闭环

利用 INS ground truth 进行离线标注和分析，但严格隔离在线运行：

- 为每帧建立 GICP 正确性标签；
- 同步记录 gating 决策及其原因；
- 统计错误接受、错误拒绝、正确接受和正确拒绝；
- 重点度量错误接受率，而非只看总体接受率；
- 统计从首次错误接受到明显漂移的时间和距离；
- 统计每次实验重置之间的稳定时间和稳定里程；
- 按道路类型、速度、转弯、天气、动态目标和地图变化分类失败；
- 建立可重复回放的典型失败数据集和版本基线。

这是后续所有改进能否被客观验证的基础。

### 第二优先级：验证时间同步、外参和去畸变

这些基础误差会同时污染地图、DLIO、GICP 和 gating。应通过静止、直行、转弯等可解释测试，先排除系统性输入问题，再优化高层算法。

### 第三优先级：控制错误传播并改造 Gating

优先实现候选状态、最后可信状态、多帧确认、回滚和多指标 gating。目标是使偶发错误接受不再立即导致整条定位链路失控。

### 第四优先级：增强 GICP 与三雷达内部一致性

实施多分辨率、多初值、多候选、动态点抑制、几何退化检测和分雷达支持度分析，降低错误局部最优出现的概率并提供更丰富的 gating 证据。

### 第五优先级：充分利用 DLIO 短时先验

将 DLIO 用于 GICP 初始化、动态搜索范围、运动一致性验证和短时拒绝传播，同时限制其长期漂移风险。

### 第六优先级：生成和维护定位专用地图

在 GLIM 高精度地图基础上，清理动态与不稳定点，保留稳定结构，建立多分辨率索引和地图质量标签，并形成地图更新闭环。

### 第七优先级：建立完整状态机和自主重定位

在稳定跟踪和可靠失效检测基础上，实现定位丢失声明、全局候选检索、粗配准、GICP 精配准及多帧恢复确认，最终替代 snap-to-INS。

## 10. 评价建议

为避免研发目标被“高接受率”误导，建议至少评估以下内容：

- 错误匹配接受率；
- 正确匹配拒绝率；
- 正确接受率和总体覆盖率；
- 定位平移、航向、横向、纵向和高度误差；
- 每公里漂移和最大连续误差；
- 首次错误接受到定位丢失的时间与距离；
- 系统检测定位丢失所需的帧数和时间；
- 无 INS 条件下的重定位成功率和耗时；
- 每次实验重置之间的稳定时间与里程；
- 不同道路结构、车速、转弯、动态目标比例和地图变化下的表现；
- 平均、较高分位和最坏情况下的处理延迟；
- CPU、GPU、内存和地图查询开销；
- Gating 置信度与真实正确概率之间的校准程度。

所有指标应分别报告“允许实验 snap-to-INS”和“完全禁止 snap-to-INS”的结果，避免将外部恢复能力误计为定位算法本身的鲁棒性。

## 11. 结论

本项目的核心任务是：利用左、中、右三个激光雷达、实时 IMU、DLIO 和 GICP，在 GLIM 离线构建的先验三维点云地图中完成车辆实时定位，并在运行时完全隔离 INS/GPS ground truth。

当前系统的主要矛盾不是 GICP 偶尔产生错误结果，而是 gating 有时会接受错误的局部最优解，使其进入正式状态并污染后续匹配初值，最终形成持续漂移。连续拒绝后 snap 到 INS 可以支持实验恢复，但不能代表系统在真实 GPS/INS 失效条件下具备自主恢复能力。

因此，系统优化不应局限于 GLIM、GICP 或 gating 中的单一模块，而应形成完整防线：首先保证时间同步、外参和去畸变正确；其次生成面向定位的稳定地图；再次充分利用三雷达内部一致性与 DLIO 短时先验，增强 GICP 并构建多指标 gating；随后通过候选状态、多帧确认、回滚和状态机阻断错误传播；最终建立不依赖 INS/GPS 的自主重定位和地图维护能力。

从科研角度，最清晰的核心问题可归纳为：

> 在不使用 INS/GPS ground truth 的条件下，如何检测多激光雷达 GICP 的错误收敛，阻止错误匹配在连续定位中传播，并在定位丢失后依靠激光雷达、IMU 与先验地图自主恢复。

这一问题同时覆盖匹配质量评估、传感器融合、不确定性管理、连续状态估计和失效恢复，能够直接对应当前系统的主要工程痛点，也具有明确的实验验证路径。
