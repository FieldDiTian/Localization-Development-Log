GLIM 在线输入改成更确定的 live 输入处理，并用可靠 QoS 消费 `/luminar_front/points`。

之前 u-turn 的直接触发点不是 LiDAR 时间戳本身坏，而是 online replay/subscription 的 best-effort LiDAR 路径会丢帧/形成 gap，GLIM 看到：

```
LiDAR gap
points-IMU time difference
```

这些会让轨迹在某些段突然坏掉。

现在改成 reliable LiDAR QoS + deep queue 后，这两个症状消失：

```
large time gap between consecutive LiDAR = 0
large time difference between points and imu = 0
IndeterminantLinearSystemException = 0
segfault = 0
```

/media/roar/data1/rosbags/putnam/may_26/run_3/glim_online_reliable_full_viewer_run3_20260622_163651/map.pcd