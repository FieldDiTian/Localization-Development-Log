仓库需要的改动（当前art-jazzy-prepmap已实现）：
把默认合同改成 local ENU `/gnss`
**新增 adapter 包**：新增 `adapter/`，用 `GeographicLib::LocalCartesian` 把 Atlas LLA 转 local ENU，发布 `/gnss`、`/gnss_rtk_fixed`、兼容 `/gps_p1/filtered_odom*` 和 `/gps_p1/imu`。origin 必须来自 `local_enu_origin`
新增 `glim_dump_to_pcd`，可以从 GLIM dump 导出 `map.pcd`；默认地图/world frame 是 local ENU


